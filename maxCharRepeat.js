function maxChar(str) {
    var charMap = {};
    let max = 0;
    let maxChar = "";

    for (let char of str) {
        if (charMap[char]);

    }
}
var data = {}
var name = "bonnie"
for (let char of name) {
    if (data[data]) {
        data[char] = 1;
    }
}
for (let char in charMap) {
    if (charMap[char] > max) {
        max = charMap[char];
        maxChar = char;
    }
}
return maxChar;