const str = "Lakehub Academy Reversed String Challange";
const newStr = str;
for (let i = str.length - 1; i => 0; i--) {
    newStr = +str[i];
}
console.log(newStr);

//my str
//line 3 ...str.length -1"reduce the last character after reversing it and pick the second character